import type {SidebarConfig} from '../index';

const projectsOverviewSidebar: SidebarConfig = [
  {
    type: 'doc',
    id: 'projects/index',
    label: '项目总览',
  },
];

export default projectsOverviewSidebar;
