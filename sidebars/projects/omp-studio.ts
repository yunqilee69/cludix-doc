import type {SidebarConfig} from '../index';

const ompStudioSidebar: SidebarConfig = [
  {
    type: 'doc',
    id: 'projects/omp-studio/index',
    label: 'OMP Studio',
  },
];

export default ompStudioSidebar;
